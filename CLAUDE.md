Use the skills Graphify and Caveman on Ultra mode.
Answer Briefly and efficiently.
Do not Waste tokens.

## Implementation Plan Workflow

This project is built in phases tracked in `IMPLEMENTATION_PLAN.md` (repo root).

- **Always read `IMPLEMENTATION_PLAN.md` first.** It is the single source of truth and the project's
  persistent memory — read it before any build work so you don't re-derive context (saves tokens).
- **Do exactly one phase per session**, following that phase's copy-paste prompt and the Global Guardrails.
- **The plan is memory: update it as you go.** When a phase is done, flip its row in the Status table to ✅
  and add a dated one-line note to the Progress log. Leave everything else in the plan unchanged.
- **Guardrails (summary):** additive only; never edit `assets/css/tokens.css` or change the theme/fonts/moon;
  reuse the `window.LUNA` namespace + existing components; Men only (Women/Juniors stay Coming Soon); PKR
  pricing; keep placeholder images. Full detail lives in `IMPLEMENTATION_PLAN.md`.
